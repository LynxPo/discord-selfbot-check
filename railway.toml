[build]
builder = "NIXPACKS"
buildCommand = "mvn clean package -DskipTests"

[deploy]
startCommand = "java -jar target/discord-selfbot.jar"
healthcheckPath = "/health"
healthcheckTimeout = 30

[variables]
PORT = "8080"
